# Scripts

## `emerald.py`

Legacy standalone tool that converts an Emerald DUB plot `.xlsx` directly to an EAI Excel workbook (not the AOS CSV format).

For the main conversion pipeline, use `python run.py` from the project root instead.

```bash
python scripts/emerald.py "plot.xlsx" -o "output.xlsx"
```
